[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "shared-er-lib"
version = "0.1.0"
description = "Shared Entity Resolution components"
authors = [{ name = "Rob" }]
dependencies = []
